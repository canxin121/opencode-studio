import { localStorageKeys } from '@/lib/persistence/storageKeys'

export const STORAGE_LAST_SESSION = localStorageKeys.chat.lastSessionId
export const STORAGE_RUN_CONFIG = localStorageKeys.chat.sessionRunConfig
