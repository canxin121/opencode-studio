This project uses multiple desktop config files:

- tauri.conf.json: default dev config
- tauri.conf.full.json: full build (bundles the server binary as the desktop backend)

In CI and local builds, pick one with `cargo tauri build --config <file>`.
